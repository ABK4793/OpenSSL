capture:
  build:
    # clean-command: mvn -B clean
    build-command: "./Configure --prefix=/usr/local/ssl --openssldir=/usr/local/ssl '-Wl,-rpath,$(LIBRPATH)' && make"

      
analyze:
  aggressiveness-level: high


  checkers:
    all-security: true
    audit: true
    # distrust-all: true
    sigma:
      enable-check-set: ["all"]
    webapp-security:
      enabled: true
